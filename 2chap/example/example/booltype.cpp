/*
#include <iostream>
using namespace std;

bool ispositive(int num)
{
	if (num <= 0)
		return false;
	else
		return true;
}

int main(void)
{
	bool ispos;
	int num;

	cout << "input number : ";
	cin >> num;

	cout << "size of bool is " << sizeof(ispos) << endl;

	ispos = ispositive(num);
	if (ispos)
		cout << "positive" << endl;
	else
		cout << "non positive" << endl;
	return 0;
}
*/